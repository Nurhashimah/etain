- provide(:title, 'Location Details')
- model_class = Location
.page-header
  %h1= t('activerecord.attributes.location..title')
%p#notice= notice

/ %dl.dl-horizontal
/   %dt= t('activerecord.attributes.location.region')+":"
/   %dd= @location.try(:region_details)

%dl.dl-horizontal
  %dt= t('activerecord.attributes.location.name')+":"
  %dd= @location.name

%dl.dl-horizontal
  %dt= t('activerecord.attributes.location.description')+":"
  %dd= @location.description

/%dl.dl-horizontal
/  %dt= t('courses.user_id')+":"
/  %dd= @course.user_id

/%dl.dl-horizontal
/  %dt= t('courses.college')+":"
/  %dd= @course.college_id

/%dl.dl-horizontal
/  %dt= t('courses.data')+":"
/  %dd= @course.data


.form-actions
  = link_to content_tag(:i, "", :class => "fa fa-arrow-left ") + " " + t('.back', :default => t("helpers.links.back")), locations_path, :class => 'btn'
  = link_to content_tag(:i, "", :class => "fa fa-edit") + " " + t('.edit', :default => t("helpers.links.edit")), |
    edit_location_path(@location), :class => 'btn'
  = link_to content_tag(:i, "", :class => "fa fa-trash-o icon-white") + " " + t('.destroy', :default => t("helpers.links.destroy")), |
    location_path(@location),                                                                                                    |
    :method => 'delete',                                                                                                           |
    :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },                   |
    :class => 'btn btn-danger'                                                                                                     |