- provide(:title, 'Customer Details')
- model_class = Customer
.page-header
  %h1= t('customers.title')
%p#notice= notice

%dl.dl-horizontal
  %dt= t('customers.id_no')+":"
  %dd= @customer.id_no

%dl.dl-horizontal
  %dt= t('customers.name')+":"
  %dd= @customer.name

%dl.dl-horizontal
  %dt= t('customers.email')+":"
  %dd= @customer.email

%dl.dl-horizontal
  %dt= t('customers.phone')+":"
  %dd= @customer.phone

%dl.dl-horizontal
  %dt= t('customers.corporate')+":"
  %dd= @customer.corporate? ? (t 'actions.yes') : (t 'actions.no')

%dl.dl-horizontal
  %dt= t('customers.company_id')+":"
  %dd= @customer.try(:clientcompany).try(:name)

/%dl.dl-horizontal
/  %dt= t('customers.user_id')+":"
/  %dd= @customer.user_id

/%dl.dl-horizontal
/  %dt= t('customers.college')+":"
/  %dd= @customer.college_id

/%dl.dl-horizontal
/  %dt= t('customers.data')+":"
/  %dd= @customer.data


.form-actions
  = link_to content_tag(:i, "", :class => "fa fa-arrow-left ") + " " + t('.back', :default => t("helpers.links.back")), customers_path, :class => 'btn'
  = link_to content_tag(:i, "", :class => "fa fa-edit") + " " + t('.edit', :default => t("helpers.links.edit")), |
    edit_customer_path(@customer), :class => 'btn'
  = link_to content_tag(:i, "", :class => "fa fa-trash-o icon-white") + " " + t('.destroy', :default => t("helpers.links.destroy")), |
    customer_path(@customer),                                                                                                    |
    :method => 'delete',                                                                                                           |
    :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },                   |
    :class => 'btn btn-danger'                                                                                                     |

