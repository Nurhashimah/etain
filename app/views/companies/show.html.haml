- provide(:title, 'Company Details')
- model_class = Company
.page-header
  %h1= t('activerecord.attributes.company.title')
%p#notice= notice

%dl.dl-horizontal
  %dt= t('activerecord.attributes.company.company_code')+":"
  %dd= @company.company_code

%dl.dl-horizontal
  %dt= t('activerecord.attributes.company.name')+":"
  %dd= @company.name

%dl.dl-horizontal
  %dt= t('activerecord.attributes.company.address')+":"
  %dd= @company.address

%dl.dl-horizontal
  %dt= t('activerecord.attributes.company.phone')+":"
  %dd= @company.phone

%dl.dl-horizontal
  %dt= t('activerecord.attributes.company.fax')+":"
  %dd= @company.fax

%dl.dl-horizontal
  %dt= t('activerecord.attributes.company.email')+":"
  %dd= @company.email

/%dl.dl-horizontal
/  %dt= t('helpers.user_id')+":"
/  %dd= @company.user_id

/%dl.dl-horizontal
/  %dt= t('helpers.college_id')+":"
/  %dd= @company.college_id

/%dl.dl-horizontal
/  %dt= t('helpers..data')+":"
/  %dd= @company.data


.form-actions
  = link_to content_tag(:i, "", :class => "fa fa-arrow-left ") + " " + t('.back', :default => t("helpers.links.back")), companies_path, :class => 'btn'
  = link_to content_tag(:i, "", :class => "fa fa-edit") + " " + t('.edit', :default => t("helpers.links.edit")), |
    edit_company_path(@company), :class => 'btn'
  = link_to content_tag(:i, "", :class => "fa fa-trash-o icon-white") + " " + t('.destroy', :default => t("helpers.links.destroy")), |
    company_path(@company),                                                                                                    |
    :method => 'delete',                                                                                                           |
    :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },                   |
    :class => 'btn btn-danger'                                                                                                     |

